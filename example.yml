test:
  type: container
  label: 
    type: state
    name: message
  state:
    message: wow
  on-click: setState(message, hello)
  spacing: 0
  orientation: horizontal
  margin: 
    left: 1
  classes:
    - hello