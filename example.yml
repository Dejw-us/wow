$message: request(get, localhost:8080)

child:
  type: button
  label: $message
  on-click: $message = 12
  style:
    classes:
      - hello
anchor:
  - top
  - left
  - right
layer: top

style:
  file: style.css
  classes: