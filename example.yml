$message: request(get, {name = 12})

child:
  type: button
  label: $message
  on-click: ~request(get, localhost:8080)
  style:
    classes:
      - hello
anchor:
  - top
  - left
  - right
layer: top

style:
  file: style.css
  classes: