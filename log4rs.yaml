# log4rs.yml

refresh_rate: 30 seconds  # Optional: reload config every 30 seconds

appenders:
  stdout:
    kind: console
    encoder:
      pattern: "{d} - {l} - {m}{n}"

root:
  level: info
  appenders:
    - stdout
